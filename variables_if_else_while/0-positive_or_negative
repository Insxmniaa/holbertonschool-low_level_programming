#include <stdio.h>

int main()
{
	int n;
	
	if(n > 0) {
		printf("%d is positive"\n);
	}

	if else(n = 0) {
		printf("%d is 0"\n);
	}

	else(n < 0) {
		printf("%d is negative"\n);
	}
	
	return (0);
}
